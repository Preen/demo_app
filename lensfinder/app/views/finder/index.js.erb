//alert('hej');

//        var category = $('#category_id');
//        category.change(function() {
//            alert(category.val());
//            var catValue = category.val();
//
//$.getJSON('/finder/' + catValue + ".json", function(data) {
//
//            var select = $('#_product_id');
//            var options = select.attr('options');
//            $('option', select).remove();
//
//            $.each(data, function(index, array) {
//                options[options.length] = new Option(array['title']);
//            });
//
//});


            //  $.ajax({
            //    type: "GET",
            //    url: "/finder/" + catValue + ".xml",
            //    data:"category_id=" + catValue,
            //    success: function(html) {
            //        $("#products").html(html);
            //    }
            //});

  //      });
